(function () {
  'use strict';

  var serviceId = 'priceService';

  angular.module('myApp').factory(serviceId, function priceService($http, $filter) {
    var preparedData = [];

    var factory = {
    };

    return factory;
  });
})();